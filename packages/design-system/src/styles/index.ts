import { breakpoints } from './common'
import { palette as colors } from './palette'
import { spacing } from './spacing'
import { typography } from './typography'

export * as animation from './animation'
export { GlobalStyle, fontUrl } from './global'

export const theme = {
  colors,
  typography,
  breakpoints,
  spacing
}
