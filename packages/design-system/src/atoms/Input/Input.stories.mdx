import { Meta, Canvas, Story, Props } from "@storybook/addon-docs/blocks";
import { Input } from "./Input";
import { IconButton } from "../IconButton";
import { Icon } from "../Icon";

<Meta
  title="Atoms/Input"
  component={Input}
  parameters={{
    controls: {
      sort: 'requiredFirst'
    }
  }}
/>

## Default
<Story name="Default">
  <div style={{maxWidth: '360px'}}>
    <Input inputName={"disabled-1"} labelText={"Label"} placeholder={'with label'}/>
    <hr />
    <Input inputName={"disabled-1"} labelText={"disabled"} hiddenLabel placeholder={'with hidden label'}/>
  </div>
</Story>

## Password
<Story name="Password">
  <div style={{maxWidth: '360px'}}>
    <Input inputName={"disabled-1"} labelText={"disabled"} type="password" placeholder={'some random placeholder'} />
     <hr />
    <Input inputName={"disabled-1"} labelText={"disabled"} type="password" placeholder={'some random placeholder'} />
  </div>
</Story>

## Custom adornment
<Story name="Custom adornment">
  <div style={{maxWidth: '360px'}}>
    <Input
      inputName={"disabled-2"}
      labelText={"custom adornment"}
      placeholder={'some random placeholder'}
      onInputChangeCallback={() => {}}
      adornment={{
        side: 'right',
        component: (
          <IconButton size="s" variant="ghost">
            <Icon name="basket" />
          </IconButton>
        )
      }}
    />
    <hr />
    <Input
      inputName={"disabled-2"}
      labelText={"custom adornment"}
      placeholder={'some random placeholder'}
      onInputChangeCallback={() => {}}
      adornment={{
        side: 'left',
        component: (
          <IconButton size="s" variant="ghost">
            <Icon name="basket" />
          </IconButton>
        )
      }}
    />
    <hr />
    <Input
      inputName={"disabled-3"}
      labelText={"custom"}
      placeholder={'some random placeholder'}
      onInputChangeCallback={() => {}}
      adornment={{
        side: 'left',
        component: (
          <IconButton  variant="contained" color="primary">
            <Icon name="basket" />
          </IconButton>
        )
      }}
    />
    <hr />
    <Input
      inputName={"disabled-5"}
      labelText={"custom"}
      hiddenLabel
      placeholder={'Search...'}
      onInputChangeCallback={() => {}}
      adornment={{
        side: 'right',
        component: (
          <IconButton size="m" variant="contained" color="secondary">
            <Icon name="basket"  />
          </IconButton>
        )
      }}
    />
    <hr />
    <Input
      inputName={"disabled-2"}
      labelText={"custom adornment"}
      placeholder={'some random placeholder'}
      onInputChangeCallback={() => {}}
      adornment={{
        side: 'left',
        component: (
          <Icon name="basket" size={16} />
        )
      }}
    />
  </div>
</Story>



Experiment with this story with the Controls addon in the Canvas tab.

export const Template = (args) => <Input {...args} />

<Props of={Input} />

## Playground

<Canvas>
  <Story
    name="Playground"
  >
    {Template.bind({})}
  </Story>
</Canvas>